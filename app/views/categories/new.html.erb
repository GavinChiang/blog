<div id="page_wrapper">
	<h1> New Category</h1>

	<%= simple_form_for @category  do |c| %>
	<div  class="field">
    <%= c.input :title %>
  </div>
	<br>
	<%= c.submit "Submit", data: { disable_with: "Submitting..." } %>
	<% end %>
	
</div>